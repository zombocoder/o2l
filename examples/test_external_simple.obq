Object Calculator {
    property value: Int
    
    constructor(initial: Int) {
        this.value = initial
    }
    
    @external method getValue(): Int {
        return this.value
    }
}

Object Main {
    method main(): Void {
        calc = new Calculator(5)
        print("Value: %d", calc.getValue())
    }
}